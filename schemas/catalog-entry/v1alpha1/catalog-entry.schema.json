{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:mission-kit:schemas:catalog-entry:catalog-entry:v1alpha1",
  "title": "Mission Kit catalogue entry frontmatter v1alpha1",
  "description": "Frontmatter contract for an ID-prefixed Mission Kit entry: axiom, style, methodology, role, pattern, domain, work-type or mission-required. Every property either changes how an agent acts on the entry or addresses the entry. Archival bookkeeping is excluded by construction: additionalProperties is false, so a reintroduced added, provenance, source-tele or retrieved field fails validation rather than accumulating unread.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "category",
    "title",
    "status",
    "hydrate-when"
  ],
  "properties": {
    "id": {
      "description": "Stable address. An entry that cannot be cited cannot be referenced from another entry or from an agent's context.",
      "type": "string",
      "pattern": "^(AR|MREQ|WT|SC|A|S|M|R|K|W|D|P|C|E)-?[0-9]+$"
    },
    "category": {
      "description": "Selects the directory that owns the entry and the ID prefix it must carry.",
      "type": "string",
      "enum": [
        "axiom",
        "style",
        "methodology",
        "role",
        "pattern",
        "domain",
        "work-type",
        "mission-required",
        "skill",
        "contract",
        "entity",
        "component",
        "artifact"
      ]
    },
    "title": {
      "description": "Human-readable name, reproduced verbatim in INDEX.md.",
      "type": "string",
      "minLength": 1
    },
    "status": {
      "description": "Whether this entry is in force, and if not, why not. This field answers that question and no other: a requirement's progress and a work-type's generatability are separate concerns with their own fields, because conflating them is how five disagreeing vocabularies arose. active: in force, apply it. draft: not yet in force, do not apply. superseded: replaced, follow the entry whose supersedes names this one. retired: no longer in force, the ID is kept so citations resolve.",
      "type": "string",
      "enum": [
        "active",
        "draft",
        "superseded",
        "retired"
      ]
    },
    "fulfilment": {
      "description": "How far a tracked requirement has got. Progress, not force: an MREQ entry is in force while it stands, whatever its progress.",
      "type": "string",
      "enum": [
        "deferred",
        "partial",
        "done",
        "wont-do"
      ]
    },
    "generatable": {
      "description": "Whether the substrate can generate this work-type from the axes, or it must be authored by hand. A capability of the type, not a lifecycle state.",
      "type": "boolean"
    },
    "hydrate-when": {
      "description": "The condition under which to open this entry, stated so an agent can evaluate it against what it is doing now. A topic never fires; see S14. Required: an entry nothing routes to has the same value as no entry.",
      "type": "string",
      "minLength": 30,
      "pattern": "^[^.!?]*[.!?]?$"
    },
    "enforced-by": {
      "description": "The tool that holds this rule, relative to the repository root. Declaring it makes the pairing checkable: a rule claiming an enforcer that does not exist is a broken promise, and an enforcer with no rule is orphaned.",
      "type": "string",
      "pattern": "^tools/"
    },
    "supersedes": {
      "description": "Entries this one replaces. Following a superseded entry is an error, and only this field says so.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "related": {
      "description": "Adjacent entries worth hydrating alongside this one. Routing, not provenance.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "applies-to": {
      "description": "Axiom scope: the class of system the mandate governs.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "source": {
      "description": "Upstream origin, permitted only where the entry is third-party content carrying a licence obligation.",
      "type": "string"
    },
    "essence": {
      "type": "string"
    },
    "engagementMode": {
      "type": "string"
    },
    "evidenceAuthorities": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "composing": {
      "description": "Whether the role composes into an arc symmetrically. Director is authority-only and does not.",
      "type": "boolean"
    },
    "separationConstraints": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "subjectSurface": {
      "type": "string"
    },
    "evidenceResolvesAgainst": {
      "type": "string"
    },
    "freeForTypes": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "pinnedForTypes": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "roleEligibility": {},
    "domainEligibility": {},
    "domainFreedom": {},
    "evidenceContract": {},
    "evidenceAuthority": {},
    "parameters": {},
    "generationMode": {},
    "falsifier": {},
    "compositionHooks": {},
    "backstop": {},
    "revival-trigger": {
      "type": "string"
    },
    "promoted-slice": {
      "type": "string"
    },
    "related-axioms": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "recurrence": {
      "description": "Evidence tier for an artifact type's cross-project recurrence. 'demonstrated' means observed in two or more independent projects; 'argued' means one instance plus a reasoned case. Recorded because a single project can show a shape was produced, never that another team would produce it.",
      "type": "string",
      "enum": [
        "demonstrated",
        "argued"
      ]
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "category": {
            "const": "axiom"
          },
          "id": {
            "pattern": "^A[1-9][0-9]*$"
          }
        },
        "required": [
          "category",
          "id"
        ]
      },
      "then": {
        "required": [
          "applies-to",
          "related"
        ]
      }
    },
    {
      "if": {
        "properties": {
          "category": {
            "const": "work-type"
          },
          "id": {
            "pattern": "^W[0-9]+$"
          }
        },
        "required": [
          "category",
          "id"
        ]
      },
      "then": {
        "required": [
          "roleEligibility",
          "evidenceContract",
          "domainEligibility"
        ]
      }
    },
    {
      "if": {
        "properties": {
          "category": {
            "const": "artifact"
          },
          "id": {
            "pattern": "^AR[1-9][0-9]*$"
          }
        },
        "required": [
          "category",
          "id"
        ]
      },
      "then": {
        "required": [
          "recurrence"
        ]
      }
    }
  ]
}
