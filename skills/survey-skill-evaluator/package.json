{
  "name": "@mission-kit/survey-skill-evaluator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Sovereign evaluator for controlled Survey-skill campaigns.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "compile": "./compile.sh",
    "build": "./compile.sh",
    "check": "./compile.sh --check",
    "validate": "./compile.sh --check --verify-package",
    "package:check": "./compile.sh --check --verify-package",
    "test": "node tests/run-manifest.mjs"
  }
}
