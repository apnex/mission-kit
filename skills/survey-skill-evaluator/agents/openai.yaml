interface:
  display_name: "Survey Skill Evaluator"
  short_description: "Run controlled, evidence-bound Survey evaluations"
  default_prompt: "Use $survey-skill-evaluator to validate and run a sealed Survey evaluation campaign."
