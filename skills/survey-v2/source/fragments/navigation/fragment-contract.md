Treat one descriptor and one payload as the smallest unit owning an
independently meaningful cognitive purpose. Resolve composition only through
declared versioned capabilities and typed dependencies. Reject duplicate IDs,
duplicate sole providers, unresolved requirements, unknown schema majors,
cycles, path escapes and generated inputs.

Treat a registered external schema closure as one typed dependency authority:
select explicit root IDs, snapshot every transitive schema and bound semantic
validator, and reject any unmanifested or dirty member. Do not repeat its
schema IDs in the package-local schema registry.

Require every fragment to be either the sole provider of a required capability
or the named provider of required coverage. Prove the claim by removal or
coverage tests. Keep runtime references flat and one link hop from `SKILL.md`.
