Let each projection recipe own every target path it produces. Reject an
unowned target, duplicate owner, canonical-source target, budget overflow or
generated input. Render canonical UTF-8 with LF endings, stable ordering, no
timestamps, hostnames or absolute paths.

Generate phase and runtime Mermaid from `survey.protocol.json`. Embed every
state, event, guard, action, mutation, authority and selector definition as
base64url canonical JSON records. Parse generated Mermaid independently,
expand TF/RF selectors, and compare the full transition tuple multiset with the
manifest.

Generate the Director FLOW from the same transitions plus typed annotation and
rejoin records. Prove all six question lanes and every Director-facing
transition without making FLOW a third state authority.
