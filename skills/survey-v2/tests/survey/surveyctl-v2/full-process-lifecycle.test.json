{
  "$schema": "urn:mission-kit:survey-v2:schema:test-evidence:v2",
  "schemaVersion": "2.0.0",
  "id": "urn:mission-kit:survey-v2:test:survey:surveyctl-v2:full-process-lifecycle",
  "obligationId": "O-R12-05",
  "requirementIds": ["R12"],
  "invariantIds": ["AS12", "AS15"],
  "verification": {
    "precondition": "Only the generated surveyctl executable, one source file, one external key registry, and an empty sessions root exist; no process retains Survey runtime objects.",
    "stimulus": "Use separate OS processes to initialize, issue the exact text view, inspect its pending authorities, populate and submit the SurveyFrame, validate replay, inspect the committed GenerationRecord, and cold-open status.",
    "expectedSemanticState": "The exact submitted SurveyFrame advances one durable run to round_1_frame_required at commit revision three, authenticated validation succeeds, generation evidence remains independently readable, and a fresh status process resumes the same state.",
    "expectedEvidenceState": "The test runner reports the literal assertion: separate surveyctl processes complete and cold-resume one durable SurveyFrame lifecycle",
    "forbiddenMutation": "The lifecycle must not depend on a shared in-memory runtime, accept a non-exact text skeleton, lose producer generation evidence, or reconstruct a different cold state.",
    "applicability": {"mode": "specific", "transports": [], "adapters": ["urn:mission-kit:survey-v2:adapter:surveyctl-process"]},
    "inspectedAuthorities": ["scripts/surveyctl.mjs", "source/authoring/survey/inspection.mjs", "source/executables/runtime/lib/surveyctl-engine.mjs"]
  },
  "behavior": "separate surveyctl processes complete and cold-resume one durable SurveyFrame lifecycle",
  "evidenceClass": "resume",
  "runner": "node:test",
  "executable": "tests/survey/surveyctl-v2/full-process-lifecycle.test.mjs",
  "fixtures": ["tests/survey/surveyctl-v2/support.mjs"],
  "prerequisites": [],
  "resultSchema": "urn:mission-kit:survey-v2:test-result:v2"
}
