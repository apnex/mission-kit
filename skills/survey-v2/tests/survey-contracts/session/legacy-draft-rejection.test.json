{
  "$schema": "urn:mission-kit:survey-v2:schema:test-evidence:v1",
  "schemaVersion": "1.0.0",
  "id": "urn:mission-kit:survey-v2:test:survey-contracts:session:legacy-draft-rejection",
  "obligationId": "O-SV10-03",
  "requirementIds": [
    "R11"
  ],
  "invariantIds": [
    "SV10"
  ],
  "verification": {
    "precondition": "A structurally valid v2 session has the exact empty neutral legacy draft carrier.",
    "stimulus": "Insert one usable legacy complex draft into the frozen v1 carrier.",
    "expectedSemanticState": "The v2 structural contract rejects the legacy draft rather than creating a second mutable product authority.",
    "expectedEvidenceState": "The test runner reports the literal assertion: the v2 session forbids legacy complex draft mirrors after authoring product handoff",
    "forbiddenMutation": "No T42 through T46 complex draft payload may persist in a v2 session.",
    "applicability": {
      "mode": "agnostic",
      "transports": [],
      "adapters": [
        "urn:mission-kit:survey-v2:adapter:session"
      ]
    },
    "inspectedAuthorities": [
      "schemas/v1/session-state.schema.json",
      "schemas/v2/session-state.schema.json"
    ]
  },
  "behavior": "the v2 session forbids legacy complex draft mirrors after authoring product handoff",
  "evidenceClass": "negative",
  "runner": "node:test",
  "executable": "tests/survey-contracts/session/legacy-draft-rejection.test.mjs",
  "fixtures": [
    "tests/fixtures/survey/session-v2/session-factory.mjs",
    "tests/survey-contracts/session/support/session-validation.mjs"
  ],
  "prerequisites": [],
  "resultSchema": "urn:mission-kit:survey-v2:test-result:v1"
}
