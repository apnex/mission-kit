{
  "$schema": "urn:mission-kit:survey-v2:schema:test-evidence:v2",
  "schemaVersion": "2.0.0",
  "id": "urn:mission-kit:survey-v2:test:survey-contracts:session:profile-resource-authority-boundary",
  "obligationId": "O-AS14-24",
  "requirementIds": [
    "R07",
    "R11"
  ],
  "invariantIds": [
    "AS14"
  ],
  "verification": {
    "precondition": "A structurally valid v2 session carries an unbound profile-owned Brief either as a top-level resource or inside a locally valid AuthoringMutation.",
    "stimulus": "Validate the top-level Brief, the nested Brief, and the nested Brief after storing and selecting a manifest that merely declares its schema and validator digests.",
    "expectedSemanticState": "Every unbound type is rejected independent of containment, and session data cannot promote declarative profile pins into executable validation authority.",
    "expectedEvidenceState": "The runner reports the literal assertion: a v2 session rejects unbound profile resources at every containment position and a stored manifest cannot self-authorize validation",
    "forbiddenMutation": "S10 must not admit an unknown resource because it is top-level, dispatch executable validation from session-controlled declarations, or pre-empt K12's trusted profile-authority boundary.",
    "applicability": {
      "mode": "agnostic",
      "transports": [],
      "adapters": [
        "urn:mission-kit:survey-v2:adapter:session"
      ]
    },
    "inspectedAuthorities": [
      "schemas/authoring/v1alpha1/authoring-profile-manifest.schema.json",
      "source/authoring/survey/session-semantics.mjs",
      "tests/fixtures/authoring/contracts/positive/authoring-mutation.json"
    ]
  },
  "behavior": "a v2 session rejects unbound profile resources at every containment position and a stored manifest cannot self-authorize validation",
  "evidenceClass": "negative",
  "runner": "node:test",
  "executable": "tests/survey-contracts/session/profile-resource-authority-boundary.test.mjs",
  "fixtures": [
    "tests/fixtures/authoring/contracts/positive/authoring-profile-manifest.json",
    "tests/fixtures/authoring/contracts/positive/authoring-mutation.json",
    "tests/fixtures/survey/session-v2/session-factory.mjs"
  ],
  "prerequisites": [],
  "resultSchema": "urn:mission-kit:survey-v2:test-result:v2"
}
